const fs=require('fs'),path=require('path'),cp=require('child_process');const dirs=['backend','frontend/js'];const files=[];function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(p.endsWith('.js'))files.push(p)}}dirs.forEach(walk);let bad=0;for(const f of files){const r=cp.spawnSync(process.execPath,['--check',f],{encoding:'utf8'});if(r.status!==0){bad++;console.error(r.stderr||r.stdout)}}console.log(`${files.length-bad}/${files.length} JavaScript files passed syntax check.`);process.exitCode=bad?1:0;
