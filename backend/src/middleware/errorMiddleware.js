function errorMiddleware(err,req,res,next){const id=req.requestId||`ev-${Date.now()}`;console.error(`[Error ${id}]`,err);if(err.name==='MulterError')return res.status(400).json({error:`File upload error: ${err.message}`,errorId:id});const statusCode=err.statusCode||500;const message=err.message||'An unexpected internal server error occurred.';res.status(statusCode).json({error:message,errorId:id,...(process.env.NODE_ENV==='development'?{stack:err.stack}: {})});}
module.exports=errorMiddleware;
