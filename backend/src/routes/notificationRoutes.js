const express=require('express');const router=express.Router();const C=require('../controllers/notificationController');const {authMiddleware}=require('../middleware/authMiddleware');router.use(authMiddleware);router.get('/stream',C.stream);router.get('/',C.list);router.patch('/:id/read',C.read);router.get('/preferences',C.prefs);router.put('/preferences',C.updatePrefs);module.exports=router;
